__pycache__/
*.pyc
.env
venv/
.venv/
get-pip.py
radar.db.bak
.claude/

# Derived fixture for design/testing — regenerate from radar.db, don't commit
# (radar.db is already tracked; a second copy only adds to the size problem
# that broke pushes on 2026-08-16)
docs/radar-sample.db

# Enrichment prototype output — fetched article text, regenerate with
# `python prototype_enrich.py fetch`
prototype_fetch.json
prototype_fetch.log
prototype_extract.json

# Classifier eval corpus — derived from radar.db, which is already tracked.
# Same rule as docs/radar-sample.db above: regenerate with `python evalset.py`
# rather than committing a second copy of data we already have.
data/evalset.json
eval_disagreements.json

# Locally vendored google-genai (54MB). Production installs it from
# requirements.txt; this exists only so the spike can run without a venv.
.vendor/

# eval_classifier --dump output, regenerate by re-running the eval
eval_stage2_valid.json
